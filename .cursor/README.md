## Cursor Project Metadata

This directory stores project-specific AI context for `python-ruckus-one`.

### Structure
- `memory/` - persistent repository knowledge for future tasks
- `rules/` - local guidance and conventions
- `skills/` - reusable task playbooks

